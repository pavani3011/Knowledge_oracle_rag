# requirements.txt — Local Knowledge Oracle

# Core LangChain stack
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-community>=0.2.0

# Vector store
chromadb>=0.5.0

# Document parsers
pypdf>=4.0.0              # PDF text extraction
unstructured>=0.14.0      # Markdown + HTML loading
markdown>=3.6             # Markdown pre-processing

# Utilities
tqdm>=4.66.0              # Progress bars in DirectoryLoader
